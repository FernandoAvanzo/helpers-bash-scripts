# Helpers Bash Scripts
Set of bash scripts to automatize some linux functions

### My Cli Roadmap

- [ ] implement a new module called `MyGradle`
- [ ] create in `MyGradle` a script to update the gradle version manually installed
